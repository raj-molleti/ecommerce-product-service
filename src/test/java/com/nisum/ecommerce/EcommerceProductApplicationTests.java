package com.nisum.ecommerce;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class EcommerceProductApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the Spring application context (controllers, services,
        // repositories, Mongo config, Swagger config) wires up correctly.
    }
}
